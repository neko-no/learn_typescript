"use strict";
const userName = 'Max';
let age = 29;
age = 35;
function add(n1, n2) {
    let result;
    result = n1 + n2;
    return result;
}
if (age >= 30) {
    // var isAdult = true;
    let isAdult = true;
}
console.log(isAdult);
